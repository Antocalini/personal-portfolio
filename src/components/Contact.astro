---
import { personalInfo } from '../data/Info';
---


<div id="contacto" class="contact-section">
    <div class="contact-container">
        <h2 class="contact-title">Contacto</h2>
        <p class="contact-description">
            ¿Interesado en trabajar juntos? Ponte en contacto conmigo a través del formulario o directamente por email.
        </p>

        <div class="contact-info">
            <div class="contact-item">
                <div class="contact-icon">
                    <i class="fas fa-envelope"></i>
                </div>
                <a href={`mailto:${personalInfo.email}`} class="contact-link">
                    {personalInfo.email}
                </a>
            </div>
            <div class="contact-item">
                <div class="contact-icon">
                    <i class="fas fa-phone"></i>
                </div>
                <a href={`tel:${personalInfo.phone}`} class="contact-link">
                    {personalInfo.phone}
                </a>
            </div>
            <div class="contact-item">
                <div class="contact-icon">
                    <i class="fas fa-map-marker-alt"></i>
                </div>
                <span class="contact-text">{personalInfo.location}</span>
            </div>
        </div>

        <div class="contact-form-container">
            <form class="contact-form" id="contact-form">
                <div class="form-row">
                    <div class="form-group">
                        <label class="form-label">Nombre</label>
                        <input
                            type="text"
                            placeholder="Tu nombre"
                            class="form-input"
                        />
                    </div>
                    <div class="form-group">
                        <label class="form-label">Email</label>
                        <input
                            type="email"
                            placeholder="tu@email.com"
                            class="form-input"
                        />
                    </div>
                </div>

                <div class="form-group">
                    <label class="form-label">Asunto</label>
                    <input
                        type="text"
                        placeholder="Asunto del mensaje"
                        class="form-input"
                    />
                </div>

                <div class="form-group">
                    <label class="form-label">Mensaje</label>
                    <textarea
                        rows="4"
                        placeholder="Escribe tu mensaje aquí"
                        class="form-textarea"
                    ></textarea>
                </div>

                <div id="form-message"></div>

                <button type="submit" class="form-submit">
                    Enviar mensaje
                </button>
            </form>
        </div>
    </div>
</div>

<script src="../utils/handleSubmit.js"></script>

<style>
.contact-section {
    position: relative;
    margin-top: 0;
    background: linear-gradient(to bottom, rgba(88, 28, 135, 0.2), rgba(59, 7, 100, 0.3), transparent);
    padding: 128px 0 80px 0;
}

.contact-container {
    position: relative;
    max-width: 768px;
    margin: 0 auto;
    padding: 0 16px;
    text-align: center;
}

@media (min-width: 640px) {
    .contact-container {
        padding: 0 24px;
    }
}

@media (min-width: 1024px) {
    .contact-container {
        padding: 0 32px;
    }
}

.contact-title {
    font-size: 2.25rem;
    font-weight: bold;
    letter-spacing: -0.025em;
    color: white;
    margin-bottom: 16px;
}

@media (min-width: 640px) {
    .contact-title {
        font-size: 3rem;
    }
}

.contact-description {
    font-size: 1.125rem;
    color: #9ca3af;
    margin-bottom: 48px;
}

.contact-info {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 32px;
    margin-bottom: 48px;
}

.contact-item {
    display: flex;
    align-items: center;
    gap: 12px;
}

.contact-icon {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background-color: rgba(88, 28, 135, 0.5);
    display: flex;
    align-items: center;
    justify-content: center;
}

.contact-icon i {
    color: #c084fc;
}

.contact-link {
    color: white;
    text-decoration: none;
    transition: color 0.3s ease;
}

.contact-link:hover {
    color: #c084fc;
}

.contact-text {
    color: white;
}

.contact-form-container {
    background-color: rgba(0, 0, 0, 0.4);
    backdrop-filter: blur(8px);
    border-radius: 12px;
    padding: 24px;
    border: 1px solid rgba(255, 255, 255, 0.1);
    box-shadow: 0 0 50px -12px rgba(147, 51, 234, 0.2);
}

@media (min-width: 640px) {
    .contact-form-container {
        padding: 32px;
    }
}

.contact-form {
    display: grid;
    gap: 24px;
}

.form-row {
    display: grid;
    gap: 24px;
}

@media (min-width: 640px) {
    .form-row {
        grid-template-columns: 1fr 1fr;
    }
}

.form-group {
    display: grid;
    gap: 8px;
}

.form-label {
    font-size: 0.875rem;
    color: #9ca3af;
}

.form-input,
.form-textarea {
    background-color: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 8px;
    padding: 8px 16px;
    color: white;
    outline: none;
    transition: box-shadow 0.3s ease;
}

.form-input::placeholder,
.form-textarea::placeholder {
    color: #6b7280;
}

.form-input:focus,
.form-textarea:focus {
    box-shadow: 0 0 0 2px #9333ea;
}

.form-textarea {
    resize: none;
}

.form-submit {
    cursor: pointer;
    background-color: #9333ea;
    color: white;
    width: 100%;
    padding: 12px 32px;
    border-radius: 8px;
    font-weight: 500;
    border: none;
    transition: background-color 0.3s ease;
}

@media (min-width: 640px) {
    .form-submit {
        width: auto;
        justify-self: center;
    }
}

.form-submit:hover {
    background-color: #7c3aed;
}

.form-message {
    display: none;
    padding: 12px 16px;
    border-radius: 8px;
    margin-top: 16px;
    font-size: 0.875rem;
}

.form-message.error {
    background-color: rgba(239, 68, 68, 0.1);
    border: 1px solid rgba(239, 68, 68, 0.3);
    color: #fca5a5;
}

.form-message.success {
    background-color: rgba(34, 197, 94, 0.1);
    border: 1px solid rgba(34, 197, 94, 0.3);
    color: #86efac;
}

.form-message ul {
    margin: 0;
    padding-left: 20px;
}

.form-message li {
    margin-bottom: 4px;
}


</style>